[phases.setup]
nixPkgs = ["php", "phpPackages.composer", "phpPackages.pcov", "phpPackages.pdo", "phpPackages.mbstring", "phpPackages.mysql"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader"]

[phases.start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
